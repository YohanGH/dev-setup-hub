# Challenge — <NN-phase>

<!--
  Written by the B side (see conventions/challenge.md).
  Never rewritten. If a second round is needed, append a "## Round 2" section.
  The artifact gets amended; this file preserves what had to be argued.
-->

**Artifact.** `<NN-phase.md>` (read at `<YYYY-MM-DD HH:MM>`)
**Challenger.** `<subagent | second session: model | external: vendor/model>`
**Passes run.** premise · evidence · omission · falsifiability

## Findings

Most severe first. Each one needs a claim, evidence, and a consequence in the
**next** phase — otherwise it is a nit.

### F1 · `blocker` · <one-line claim>

**Evidence.** <file:line, a quoted line from the artifact, or command output>
**Consequence.** <what phase NN+1 builds wrong if this ships as written>
**Smallest fix.** <one sentence>

### F2 · `major` · <one-line claim>

**Evidence.**
**Consequence.**
**Smallest fix.**

## Nits

Preferences. Listed so they are visibly seen and passed over; never acted on
without a human's say-so.

- <one line each>

## Not checked

Honesty about coverage. A challenge that claims total coverage is not one.

- <what could not be verified, and why>

## Verdict

`accept` — <one sentence>

<!--
  accept  → artifact stands as written
  revise  → findings change the artifact, not its direction
  reject  → the premise is wrong; re-run the phase from scratch
-->
